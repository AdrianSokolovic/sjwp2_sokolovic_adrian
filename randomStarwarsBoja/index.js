import names from "starwars-names";
var randomName = names.random();

import randomColor from "randomcolor";
var color = randomColor();

console.log(`Moje ime je ${randomName}, a najdraža boja je ${color}`);